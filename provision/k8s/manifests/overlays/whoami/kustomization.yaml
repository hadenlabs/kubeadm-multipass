apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - ingress.yaml
  - deployment.yaml
  - svc.yaml
  - grafana.yaml

commonLabels:
  env: local

namespace: whoami
